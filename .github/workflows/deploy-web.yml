name: Deploy Orison Web to GitHub Pages

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'samples/orison-web/**'
  pull_request:
    branches: [ "main", "master" ]
    paths:
      - 'samples/orison-web/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: samples/orison-web/package-lock.json

    - name: Install dependencies
      working-directory: samples/orison-web
      run: npm ci

    - name: Build
      working-directory: samples/orison-web
      run: npm run build

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./samples/orison-web/dist
        destination_dir: orison-web
